# Emerge Conflict Resolve

When you run emerge and got notification about conflicting dependencies,
automatically emerge those packages.


## Using

Call `emerge.py`, pass any arguments you'd pass to emerge.
